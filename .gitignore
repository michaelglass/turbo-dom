# Rust (Cargo.lock IS committed — this is a shippable addon, not a library)
/target

# Node
node_modules

# napi build outputs (platform binaries — rebuilt locally / in CI)
*.node

# wasm build output
*.wasm

# wasm-pack generated package
/pkg

# personal publish cheat sheet (do not commit)
NPM-PUBLISH-TURBODOM.md
